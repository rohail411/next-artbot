import React from 'react';
import PropTypes from 'prop-types';

function MobileControls({}) {
    return <div className="video-controls-mobile"></div>;
}

MobileControls.propTypes = {};

export default MobileControls;
